# Cybera Auto Mashup

A zero-input, fully automatic music mashup generator. Run the script and it synthesizes drums, bass, and melodic textures into a WAV file.

## Usage

```bash
python main.py
```

The script writes a stereo file to `output/mashup.wav` using only the Python standard library.
